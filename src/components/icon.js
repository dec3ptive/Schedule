import React from 'react';

const Icon = (icon) => {
    return (
        <div className="icon, className">
           <i className={`${icon} ${classname}`}></i> 
        </div>
    )
}

export default Icon